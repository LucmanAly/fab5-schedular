node_modules
dist
.env
.env.local
.DS_Store

# Claude Code worktree-isolation scratch dirs — ephemeral, session-local git
# worktrees. One got committed as a stray submodule reference (no matching
# .gitmodules entry) and broke Netlify's checkout; never track this path.
.claude/worktrees/

# Graphify: cache + HTML viz are large/regenerable; GRAPH_REPORT.md and
# graph.json ARE versioned so any session can read the graph's findings.
graphify-out/cache/
graphify-out/graph.html
graphify-out/manifest.json
graphify-out/.graphify_analysis.json
graphify-out/.graphify_labels.json*
# dated snapshot folders written by graphify --update (regenerable)
graphify-out/20??-??-??/
